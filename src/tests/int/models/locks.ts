export const LOCK_DEFAULT_FIELDS = {
    entryId: expect.any(String),
    expiryDate: expect.any(String),
    startDate: expect.any(String),
    lockId: expect.any(String),
    lockToken: expect.any(String),
    login: expect.any(String),
};
