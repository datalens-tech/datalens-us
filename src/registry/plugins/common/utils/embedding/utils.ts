import type {CheckEmbedding, GetEmbeddingWorkbookId} from './types';

export const checkEmbedding: CheckEmbedding = () => false;

export const getEmbeddingWorkbookId: GetEmbeddingWorkbookId = () => null;
